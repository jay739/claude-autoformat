name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shellcheck
        run: shellcheck hooks/autoformat.sh install.sh
      - name: Smoke tests
        run: bash tests/smoke.sh
